#!/usr/bin/env bun

require('../src/custom-paper-handler/cli/state_machine_cli')
  .main()
  .then((code) => {
    process.exitCode = code;
  })
  .catch((error) => {
    console.error(error);
    process.exit(1);
  });
