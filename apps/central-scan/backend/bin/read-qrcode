#!/usr/bin/env tsx

import { main } from '../src/cli/read-qrcode.ts';
main(process.argv, {
  stdin: process.stdin,
  stdout: process.stdout,
  stderr: process.stderr,
}).then((code) => {
  process.exitCode = code;
});
