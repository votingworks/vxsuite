/**
 * This migration is intentionally left blank. It used to seed data for local development when
 * NODE_ENV=development and AUTH_ENABLED=false. We've since moved that functionality to a script
 * outside of the migrations framework, but we still need this file intact for the migration chain
 * of record. Without this file, Heroku deploys will error.
 */
exports.up = () => {};
