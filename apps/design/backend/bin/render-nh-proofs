#!/usr/bin/env node

require('esbuild-runner').install({ type: 'transform' });
require('../scripts/render_nh_proofs')
  .main(process.argv.slice(2))
  .then((exitCode) => {
    process.exitCode = exitCode;
  })
  .catch((e) => {
    console.error(e);
    process.exit(1);
  });
