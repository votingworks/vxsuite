{
  "name": "@votingworks/grout-test-utils",
  "version": "0.1.0",
  "private": true,
  "description": "Test utilities for testing apps using Grout",
  "license": "GPL-3.0-only",
  "type": "module",
  "exports": {
    ".": {
      "types": "./build/index.d.ts",
      "default": "./build/index.js"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "turbo run build:self --filter=$npm_package_name",
    "build:self": "tsc --build tsconfig.build.json",
    "clean": "turbo run clean:self --filter=$npm_package_name...",
    "clean:self": "rm -rf build",
    "lint": "pnpm type-check && eslint --cache .",
    "lint:fix": "pnpm type-check && eslint --cache . --fix",
    "test": "vitest",
    "test:run": "vitest run",
    "type-check": "tsc --build"
  },
  "dependencies": {
    "@votingworks/test-utils": "workspace:*"
  },
  "devDependencies": {
    "@vitest/coverage-istanbul": "^4.1.6",
    "@votingworks/grout": "workspace:*",
    "eslint-plugin-vx": "workspace:*",
    "expect-type": "^0.15.0",
    "sort-package-json": "^1.50.0",
    "vitest": "^4.1.6"
  },
  "peerDependencies": {
    "@votingworks/grout": "workspace:*"
  },
  "packageManager": "pnpm@10.34.5"
}
