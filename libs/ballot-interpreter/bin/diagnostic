#!/usr/bin/env node

// @ts-check

// Runs the compiled output; run `pnpm build:self` first.
import { main } from '../build/bubble-ballot-ts/diagnostic_cli.js';

main(process.argv.slice(2)).catch((err) => {
  console.error(err.message);
  process.exit(1);
});
