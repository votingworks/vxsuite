import { defineConfig } from '../../vitest.config.shared.mjs';

export default defineConfig({
  test: {
    coverage: {
      // src/cdf/** is generated by cdf-schema-builder — coverage of its
      // output is noise, not signal
      exclude: ['src/index.ts', 'src/cdf/**'],
      // Remove after migration to coverage-check is complete
      thresholds: {
        lines: 0,
        branches: 0,
      },
    },
  },
});
