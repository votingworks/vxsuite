[package]
name = "vx-logging"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[lib]
path = "types-rust/src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
