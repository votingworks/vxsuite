#!/usr/bin/env -S npx tsx

import { main } from '../src/cli.ts';
main(process.argv)
  .then((code) => {
    process.exitCode = code;
  })
  .catch((error) => {
    console.error(error);
    process.exit(1);
  });
