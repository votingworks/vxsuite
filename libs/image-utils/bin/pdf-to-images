#!/usr/bin/env node

// @ts-check

// Runs the compiled output; run `pnpm build:self` first.
import { main } from '../build/cli/pdf_to_images.js';

main(process.argv.slice(2), {
  stderr: process.stderr,
});
