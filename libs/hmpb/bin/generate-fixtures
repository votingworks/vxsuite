#!/usr/bin/env -S npx tsx

import { main } from '../src/generate_fixtures.ts';
main()
  .then((exitCode) => {
    process.exitCode = exitCode;
  })
  .catch((e) => {
    console.error(e);
    process.exit(1);
  });
