#!/usr/bin/env node

require('esbuild-runner/register');

process.exitCode = require('../src/cli/generate-sample-cvr-file').main(
  process.argv,
  {
    stdin: process.stdin,
    stdout: process.stdout,
    stderr: process.stderr,
  }
);
