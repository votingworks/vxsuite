import { join } from 'path'
import { Fixture } from '../../fixtures'

export const foldMakesContestTooWide = new Fixture(
  join(__dirname, 'fold-makes-contest-too-wide.png')
)
export const strayMarkMakesContestTooWide = new Fixture(
  join(__dirname, 'stray-mark-makes-contest-too-wide.png')
)
