#!/usr/bin/env node

// Runs the compiled output; run `pnpm build:self` first.
import { main } from '../build/cli/generate-voters/index.js';

process.exitCode = main(process.argv, {
  stdin: process.stdin,
  stdout: process.stdout,
  stderr: process.stderr,
});
