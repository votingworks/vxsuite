#!/usr/bin/env -S npx tsx

import { main } from '../src/cli/generate-election.ts';
process.exitCode = main(process.argv, {
  stdin: process.stdin,
  stdout: process.stdout,
  stderr: process.stderr,
});
