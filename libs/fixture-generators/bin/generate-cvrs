#!/usr/bin/env -S npx tsx

import { main } from '../src/cli/generate-cvrs.ts';
main(process.argv, {
  stdin: process.stdin,
  stdout: process.stdout,
  stderr: process.stderr,
}).then((exitCode) => {
  process.exitCode = exitCode;
});
