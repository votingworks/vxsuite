#!/usr/bin/env tsx

import { main } from '../src/cli/generate-election-package.ts';
main(process.argv, {
  stdin: process.stdin,
  stdout: process.stdout,
  stderr: process.stderr,
}).catch((e) => {
  console.error(e);
  process.exit(1);
});
