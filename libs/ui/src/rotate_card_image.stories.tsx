import { Meta } from '@storybook/react';

import { RotateCardImage } from './rotate_card_image';

const meta: Meta<typeof RotateCardImage> = {
  title: 'libs-ui/RotateCardImage',
  component: RotateCardImage,
};

export default meta;

export { RotateCardImage };
