import { Meta } from '@storybook/react';

import { LoadingAnimation } from './loading_animation';

const meta: Meta<typeof LoadingAnimation> = {
  title: 'libs-ui/LoadingAnimation',
  component: LoadingAnimation,
};

export default meta;

export { LoadingAnimation };
