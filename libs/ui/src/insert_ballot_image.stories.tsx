import { Meta } from '@storybook/react';

import { InsertBallotImage } from './insert_ballot_image';

const meta: Meta<typeof InsertBallotImage> = {
  title: 'libs-ui/Images',
  component: InsertBallotImage,
};

export default meta;

export { InsertBallotImage };
