#!/usr/bin/env node

require('esbuild-runner').install({
  type: 'transform',
});

require('../src/cli/add_metadata_text_to_back_page')
  .main(process.argv.slice(2))
  .then((code) => {
    process.exitCode = code;
  })
  .catch((err) => {
    console.error(err.stack);
    process.exit(1);
  });
