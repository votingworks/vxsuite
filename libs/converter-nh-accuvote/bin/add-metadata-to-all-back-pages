#!/usr/bin/env bash

if [ "${#}" -ne 1 ]; then
    echo "Usage: $0 <input-dir>"
    exit 1
fi

echo "Adding town/ward name to all back pages..."
find "${1}" -name PRINT-* -exec ./bin/add-metadata-text-to-back-page {} {} \;
