#!/usr/bin/env bash
if [ "$#" -ne 2 ]; then
    echo "Usage: $0 <input-dir> <output-dir>"
    exit 1
fi
for file in $(find $1 -name '*.pdf'); do
    OUT_DIR="$2/$(basename $(dirname $file))"
    mkdir -p $OUT_DIR
    ./bin/add-metadata-text-to-back-page $file "$OUT_DIR/$(basename $file)"
done