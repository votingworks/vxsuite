{
  "compilerOptions": {
    // Based on the current version of NodeJS for any backend apps and
    // Electron/NodeJS for frontend apps.
    "target": "ES2019",

    // Enable all strict mode checks
    "strict": true
  }
}
