#!/usr/bin/env node

// @ts-check

require('esbuild-runner').install({
  type: 'transform',
});

require('./src/prod-build').main({
  stdin: process.stdin,
  stdout: process.stdout,
  stderr: process.stderr,
});
