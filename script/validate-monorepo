#!/usr/bin/env tsx

import { main } from './src/validate-monorepo/cli.ts';

main({
  stdin: process.stdin,
  stdout: process.stdout,
  stderr: process.stderr,
})
  .catch((error) => {
    console.error(error.stack);
    return 1;
  })
  .then((exitCode) => {
    process.exitCode = exitCode;
  });
