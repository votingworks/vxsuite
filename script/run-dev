#!/usr/bin/env tsx

import { main } from './src/run-dev/index.ts';

main(process.argv, {
  stdout: process.stdout,
  stderr: process.stderr,
})
  .then((exitCode) => {
    process.exitCode = exitCode;
  })
  .catch((error) => {
    console.error(error.stack);
    process.exit(1);
  });
